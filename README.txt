0> chmod 755 *.pl && chmod 755 *.pm

1> Execute solutions.pl to see the answer to all the questions.

2> search.pl is used to search and replace the files in specified directory
Pass a comma seperated list of directories "dir1, dir2" if you want to search for files in 2 different directories

3> SearchFiles.pm is the perl module where the main search is performed.

4> test.pl is used to run the UNIT test TEST::MORE.

